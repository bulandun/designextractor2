import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Vercel handles production output/tracing automatically for Next.js projects.
  // Keeping default output avoids unnecessary standalone tracing work during deploys.
};

export default nextConfig;
